import { Validar3Directive } from './validar3.directive';

describe('Validar3Directive', () => {
  it('should create an instance', () => {
    const directive = new Validar3Directive();
    expect(directive).toBeTruthy();
  });
});
