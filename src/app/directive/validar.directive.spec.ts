import { ValidarDirective } from './validar.directive';

describe('ValidarDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarDirective();
    expect(directive).toBeTruthy();
  });
});
